# Agents

Read `CLAUDE.md` — single source: the loop (spec → implement → verify → review → ship), must-nevers, GitHub rules.

PR bodies must include, before create/update:

```markdown
## Risk Assessment

**Level:** Low | Medium | High
**Rationale:** <why this level fits the change>
```
